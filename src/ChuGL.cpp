//-----------------------------------------------------------------------------
// Entaro ChucK Developer!
// This is a Chugin boilerplate, generated by chuginate!
//-----------------------------------------------------------------------------

// this should align with the correct versions of these ChucK files
#include "chuck_dl.h"
#include "chuck_def.h"

// general includes
#include <stdio.h>
#include <limits.h>

#include <iostream>

// declaration of chugin constructor
CK_DLL_CTOR(chugl_ctor);
// declaration of chugin desctructor
CK_DLL_DTOR(chugl_dtor);

// example of getter/setter
CK_DLL_MFUN(chugl_setParam);
CK_DLL_MFUN(chugl_getParam);

// for Chugins extending UGen, this is mono synthesis function for 1 sample
CK_DLL_TICK(chugl_tick);

// this is a special offset reserved for Chugin internal data
t_CKINT chugl_data_offset = 0;


// class definition for internal Chugin data
// (note: this isn't strictly necessary, but serves as example
// of one recommended approach)
class ChuGL
{
public:
    // constructor
    ChuGL( t_CKFLOAT fs)
    {

        m_param = 0;
    }

    // for Chugins extending UGen
    SAMPLE tick( SAMPLE in )
    {
        // default: this passes whatever input is patched into Chugin
        return in;
    }

    // set parameter example
    t_CKFLOAT setParam( t_CKFLOAT p )
    {
        std::cerr << "FOO!" << std::endl;
        m_param = p;
        return p;
    }

    // get parameter example
    t_CKFLOAT getParam() { return m_param; }
    
private:
    // instance data
    t_CKFLOAT m_param;
};


// query function: chuck calls this when loading the Chugin
// NOTE: developer will need to modify this function to
// add additional functions to this Chugin
CK_DLL_QUERY( ChuGL )
{
    // hmm, don't change this...
    QUERY->setname(QUERY, "ChuGL");
    
    // begin the class definition
    // can change the second argument to extend a different ChucK class
    QUERY->begin_class(QUERY, "ChuGL", "UGen");

    // register the constructor (probably no need to change)
    QUERY->add_ctor(QUERY, chugl_ctor);
    // register the destructor (probably no need to change)
    QUERY->add_dtor(QUERY, chugl_dtor);
    
    // for UGen's only: add tick function
    QUERY->add_ugen_func(QUERY, chugl_tick, NULL, 1, 1);
    
    // NOTE: if this is to be a UGen with more than 1 channel, 
    // e.g., a multichannel UGen -- will need to use add_ugen_funcf()
    // and declare a tickf function using CK_DLL_TICKF

    // example of adding setter method
    QUERY->add_mfun(QUERY, chugl_setParam, "float", "param");
    // example of adding argument to the above method
    QUERY->add_arg(QUERY, "float", "arg");

    // example of adding getter method
    QUERY->add_mfun(QUERY, chugl_getParam, "float", "param");
    
    // this reserves a variable in the ChucK internal class to store 
    // referene to the c++ class we defined above
    chugl_data_offset = QUERY->add_mvar(QUERY, "int", "@cgl_data", false);

    // end the class definition
    // IMPORTANT: this MUST be called!
    QUERY->end_class(QUERY);

    // wasn't that a breeze?
    return TRUE;
}


// implementation for the constructor
CK_DLL_CTOR(chugl_ctor)
{
    // get the offset where we'll store our internal c++ class pointer
    OBJ_MEMBER_INT(SELF, chugl_data_offset) = 0;
    
    // instantiate our internal c++ class representation
    ChuGL * cgl_obj = new ChuGL(API->vm->get_srate(API, SHRED));
    
    // store the pointer in the ChucK object member
    OBJ_MEMBER_INT(SELF, chugl_data_offset) = (t_CKINT) cgl_obj;
}


// implementation for the destructor
CK_DLL_DTOR(chugl_dtor)
{
    // get our c++ class pointer
    ChuGL * cgl_obj = (ChuGL *) OBJ_MEMBER_INT(SELF, chugl_data_offset);
    // check it
    if( cgl_obj )
    {
        // clean up
        delete cgl_obj;
        OBJ_MEMBER_INT(SELF, chugl_data_offset) = 0;
        cgl_obj = NULL;
    }
}


// implementation for tick function
CK_DLL_TICK(chugl_tick)
{
    // get our c++ class pointer
    ChuGL * cgl_obj = (ChuGL *) OBJ_MEMBER_INT(SELF, chugl_data_offset);
 
    // invoke our tick function; store in the magical out variable
    if(cgl_obj) *out = cgl_obj->tick(in);

    // yes
    return TRUE;
}


// example implementation for setter
CK_DLL_MFUN(chugl_setParam)
{
    // get our c++ class pointer
    ChuGL * cgl_obj = (ChuGL *) OBJ_MEMBER_INT(SELF, chugl_data_offset);
    // set the return value
    RETURN->v_float = cgl_obj->setParam(GET_NEXT_FLOAT(ARGS));
}


// example implementation for getter
CK_DLL_MFUN(chugl_getParam)
{
    // get our c++ class pointer
    ChuGL * cgl_obj = (ChuGL *) OBJ_MEMBER_INT(SELF, chugl_data_offset);
    // set the return value
    RETURN->v_float = cgl_obj->getParam();
}
