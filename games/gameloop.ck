while (1) {
    vec2 screen_size;
    // .5 => float BORDER_PADDING;
    // @(BORDER_PADDING, BORDER_PADDING) +=> screen_size;
    screen_size - @(1, 2) => screen_size;

    float f;
    1.0 -=> f;
    // 2 /=> screen_size;
}
